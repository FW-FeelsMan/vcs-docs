@page "/Content/Users/user_closed_tickets"
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Policy = "SupportDeskAccess")]
@model VCS_DOCs.Support.Pages.Content.Users.UserClosedTicketsModel
@inject Microsoft.AspNetCore.Antiforgery.IAntiforgery Antiforgery
@{
    Layout = null;
    ViewData["Title"] = "Закрытые заявки";
}
<link rel="stylesheet" href="~/css/sub_css/support_common_table.css" asp-append-version="true" />
<!-- Можно переиспользовать стили открытых заявок -->
<link rel="stylesheet" href="~/css/sub_css/user_opentickets_css/user_opentickets.css" asp-append-version="true" />
<meta name="csrf-token" content="@Antiforgery.GetAndStoreTokens(HttpContext).RequestToken" />

<style>
    .status-badge.closed {
        border-color: #9aa0a6;
    }
</style>

<div id="user-closed-tickets" class="profile_background accounts-page tickets-page">
    <div class="button-container">
        <h2>Закрытые заявки</h2>

        <div class="inline-controls">
            <input id="user_searchBox" class="user_searchBox" type="text" placeholder="Поиск (№ или тема)" />
            <button id="btn-search" class="button-sliding small">Найти</button>
        </div>
    </div>

    <hr class="solid" />

    <div class="content-scrollable">
        <table class="file-table" id="ticketsTable">
            <colgroup>
                <col style="width:80px" />
                <col style="width:200px" />
                <col style="width:200px" />
                <col style="width:140px" />
                <col style="width:130px" />
                <col style="width:170px" />
                <col style="width:150px" />
            </colgroup>
            <thead>
                <tr>
                    <th>№</th>
                    <th>Тема</th>
                    <th>Статус</th>
                    <th>Создано</th>
                    <th>Обновлено</th>
                    <th>E-mail уведомления</th>
                    <th>Действие</th>
                </tr>
            </thead>
            <tbody id="userClosedTicketsBody"></tbody>
            <tfoot class="table-spacer">
                <tr><td colspan="7"></td></tr>
            </tfoot>
        </table>
    </div>
</div>
